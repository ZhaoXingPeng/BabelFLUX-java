# This file is read by Docker Compose on the server. Keep the real .env outside Git.
# Generate each value with `openssl rand -hex 32`; the MySQL bootstrap rejects
# non-alphanumeric app/migrator passwords so it never interpolates unsafe SQL.
MYSQL_ROOT_PASSWORD=
MYSQL_APP_PASSWORD=
MYSQL_MIGRATOR_PASSWORD=
RABBITMQ_PASSWORD=
