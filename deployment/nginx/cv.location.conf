# Include inside each HTTPS server block after placing the authorized PDF at this path.
location = /cv {
    alias /opt/babelflux/public/cv.pdf;
    default_type application/pdf;
    add_header Content-Disposition "inline" always;
    add_header X-Content-Type-Options nosniff always;
}
